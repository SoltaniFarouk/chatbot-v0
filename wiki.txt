docker ps -a
docker exec -it chatbot_v1-app-1 bash
docker exec -it chatbot_v2-app-1 bash

docker exec -it chatbot_v2-db-1 mysql -u user -ppassword
docker exec -it chatbot_v0-db-1 mysql -u user -ppassword

docker-compose up --build -d
docker-compose up -d
docker-compose down
docker-compose restart
docker exec -it chatbot_v0-app-1 mysql -u user -ppassword

docker exec chatbot_v0-db-1 mysqldump -u user -ppassword appdb > database/init.sql
type database\init.sql


Invoke-RestMethod -Uri "http://localhost:8080/api/user/fast" `
-Method POST `
-ContentType "application/json" `
-Body '{"email":"john@test.com"}'

------------- omposer and the autoloader. PSR-4 ---  
cat > /var/www/composer.json << 'EOF'
{
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    }
}
EOF

apt update && apt install -y curl unzip git
curl -sS https://getcomposer.org/installer | php
cd /var/www && php composer.phar dump-autoload